# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
frontend/node_modules/
backend/node_modules/
.pnp
.pnp.js

# Testing
/coverage

# Production builds (specific to frontend)
frontend/build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.backup

# Firebase configuration and logs
.firebase/
.firebaserc
firebase.json
firebase-debug.log*
firestore-debug.log*
ui-debug.log*

# Logs
npm-debug.log*
yarn-debug.log*  # Removido, irrelevante se usar apenas npm
yarn-error.log*  # Removido, irrelevante se usar apenas npm

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
Thumbs.db
ehthumbs.db

# Package lock file (choose to ignore or not based on team workflow)
# package-lock.json  # Descomente se quiser ignorar

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file (removido, irrelevante para npm)
# .yarn-integrity

# External directories
evolution-api/

# Windows-specific temporary files
~$*
*.bak